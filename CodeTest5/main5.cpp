#include <iostream>
#include <vector>
#include <map>
#include <string>
#include <algorithm>

#define MAX_N 10000
#define MAX_M 10000
#define MAX_C 10000

#define FOR(i,n) for( int i = 0 ; i < n ; i++)

using namespace std;

vector<int> myVector;
map<int, string> myMap;

int main() {
	int T, N;
	cin >> T;
	while (T--) {
		cin >> N;
		FOR(i, N) {

		}
	}
	return 0;
}